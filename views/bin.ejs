<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="/stylesheets/style.css" />
    <title>Products</title>
  </head>
  <body>
    <%- include('head') %> <% if (data) { %>
    <div class="card">
      <img src="<%= data.image %>" alt="<%= data.title %>" />
      <h2><%= data.title %></h2>
      <p><%= data.price %> $</p>
    </div>
    <% } else { %>
    <p>Корзина пуста</p>
    <% } %>
  </body>
</html>
